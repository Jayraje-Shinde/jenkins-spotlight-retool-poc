export interface rawContributor {
  slug?: string;
  name?: string;
  location?: string;
  intro?: string;
  html?: string;
}

export interface Contributor {
  slug: string;
  name: string;
  location: string;
  intro: string;
  html: string;
}